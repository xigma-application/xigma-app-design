// others
import { DEFAULT_EXPORT_SETTING } from '../../../constants';

// types
import { TExportSetting } from '../../../types';

export const createExportSetting = (): TExportSetting => ({ ...DEFAULT_EXPORT_SETTING });
