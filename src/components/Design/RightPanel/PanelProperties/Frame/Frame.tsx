import { FC, Fragment } from 'react';

// components
import FrameHeader from './FrameHeader/FrameHeader';

const Frame: FC = () => (
  <Fragment>
    <FrameHeader />
  </Fragment>
);

export default Frame;
