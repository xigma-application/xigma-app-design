// types
import { StrokeSides } from 'types/design/enums';

export const STROKE_WEIGHT_MIN = 0;
export const STROKE_WEIGHT_MAX = 1000;

export const STROKE_SIDES_ORDER = [
  StrokeSides.all,
  StrokeSides.top,
  StrokeSides.bottom,
  StrokeSides.left,
  StrokeSides.right,
  StrokeSides.custom,
];
