export enum EffectType {
  backgroundBlur = 'backgroundBlur',
  dropShadow = 'dropShadow',
  glass = 'glass',
  innerShadow = 'innerShadow',
  layerBlur = 'layerBlur',
  noise = 'noise',
  shader = 'shader',
  texture = 'texture',
}
