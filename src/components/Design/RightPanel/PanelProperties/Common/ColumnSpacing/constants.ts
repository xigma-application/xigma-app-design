// others
import { translationNameSpace as parentNameSpace } from '../constants';

export const translationNameSpace = `${parentNameSpace}.columnSpacing`;

export const SPACING_MIN = -Number.MAX_SAFE_INTEGER;
export const SPACING_MAX = Number.MAX_SAFE_INTEGER;
export const SPACING_EQUALITY_TOLERANCE = 0.01;
