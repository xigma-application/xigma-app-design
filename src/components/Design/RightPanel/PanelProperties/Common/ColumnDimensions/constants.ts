// others
import { translationNameSpace as parentNameSpace } from '../constants';

export const translationNameSpace = `${parentNameSpace}.columnDimensions`;

export const DIMENSIONS_MIN = 1;
export const DIMENSIONS_MAX = Number.MAX_SAFE_INTEGER;
