// others
import { translationNameSpace as parentNameSpace } from '../constants';

export const translationNameSpace = `${parentNameSpace}.accountMenu`;

export const CURRENT_USER_NAME = 'Xigma (You)';
