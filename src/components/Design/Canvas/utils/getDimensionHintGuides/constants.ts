// types
import { TDimensionHintGuides } from './types';
import { TPoint } from 'types/canvas';

export const EMPTY_DIMENSION_HINT_GUIDES: TDimensionHintGuides = { labels: [], lines: [] };

export const ORIGIN: TPoint = { x: 0, y: 0 };
