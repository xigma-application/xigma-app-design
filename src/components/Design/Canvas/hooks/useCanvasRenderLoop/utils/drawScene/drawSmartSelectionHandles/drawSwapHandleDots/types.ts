// types
import { TSmartSelectionLayout } from 'types/design/smartSelection/types';

export type TDotBuffer = {
  buffer: WebGLBuffer;
  layout: TSmartSelectionLayout;
  vertexCount: number;
  zoom: number;
};
