// others
import { translationNameSpace as parentNameSpace } from '../constants';

export const translationNameSpace = `${parentNameSpace}.themeMenu`;

export const THEME_MENU_LIGHT_KEY = `${translationNameSpace}.light`;
export const THEME_MENU_DARK_KEY = `${translationNameSpace}.dark`;
export const THEME_MENU_SYSTEM_THEME_KEY = `${translationNameSpace}.systemTheme`;
