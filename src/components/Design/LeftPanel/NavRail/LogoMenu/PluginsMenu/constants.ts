// others
import { translationNameSpace as parentNameSpace } from '../constants';

export const translationNameSpace = `${parentNameSpace}.pluginsMenu`;

export const PLUGINS_MENU_RUN_LAST_PLUGIN_KEY = `${translationNameSpace}.runLastPlugin`;
export const PLUGINS_MENU_MANAGE_PLUGINS_KEY = `${translationNameSpace}.managePlugins`;
