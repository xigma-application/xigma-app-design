// others
import { translationNameSpace as parentNameSpace } from '../constants';

export const translationNameSpace = `${parentNameSpace}.zoomToMenu`;

export const ZOOM_TO_MENU_PERCENTAGE_KEY = `${translationNameSpace}.percentage`;
