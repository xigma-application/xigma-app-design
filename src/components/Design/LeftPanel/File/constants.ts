// others
import { translationNameSpace as parentNameSpace } from '../constants';

export const translationNameSpace = `${parentNameSpace}.file`;

export const DEFAULT_FILE_NAME_KEY = `${translationNameSpace}.defaultName`;
