export type TDraftRect = {
  height: number;
  width: number;
  x: number;
  y: number;
};

export type TEditingTextBox = TDraftRect;

export type TPoint = {
  x: number;
  y: number;
};

export type TLineSegment = {
  x1: number;
  x2: number;
  y1: number;
  y2: number;
};
