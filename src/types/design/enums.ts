export enum NodeType {
  ellipse = 'ellipse',
  frame = 'frame',
  line = 'line',
  media = 'media',
  path = 'path',
  polygon = 'polygon',
  rectangle = 'rectangle',
  section = 'section',
  star = 'star',
  text = 'text',
}

export enum PathType {
  ellipse = 'ellipse',
}

export enum ToolName {
  comment = 'comment',
  default = 'default',
  ellipse = 'ellipse',
  frame = 'frame',
  hand = 'hand',
  line = 'line',
  media = 'media',
  polygon = 'polygon',
  rectangle = 'rectangle',
  scale = 'scale',
  section = 'section',
  slice = 'slice',
  star = 'star',
  text = 'text',
  textOnPath = 'textOnPath',
}
