export enum NodeType {
  ellipse = 'ellipse',
  frame = 'frame',
  line = 'line',
  media = 'media',
  path = 'path',
  polygon = 'polygon',
  rectangle = 'rectangle',
  section = 'section',
  star = 'star',
  text = 'text',
  vector = 'vector',
}

export enum PathType {
  ellipse = 'ellipse',
}

export enum ToolName {
  arrow = 'arrow',
  comment = 'comment',
  default = 'default',
  ellipse = 'ellipse',
  frame = 'frame',
  hand = 'hand',
  line = 'line',
  media = 'media',
  pen = 'pen',
  pencil = 'pencil',
  polygon = 'polygon',
  rectangle = 'rectangle',
  scale = 'scale',
  section = 'section',
  slice = 'slice',
  star = 'star',
  text = 'text',
  textOnPath = 'textOnPath',
}
