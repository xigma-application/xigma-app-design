// others
import { DEFAULT_CONTRAST_CHECKER_STATE } from '../constants';

// types
import { TContrastCheckerState } from '../types';

export const contrastCheckerStateCache: { current: TContrastCheckerState } = { current: DEFAULT_CONTRAST_CHECKER_STATE };
