// types
import { TVideoPanelState } from './types';

export const translationNameSpace = 'colorPicker.video';

export const DEFAULT_VIDEO_PANEL_STATE: TVideoPanelState = {
  fillMode: 'fill',
  videoSrcUrl: null,
  videoUrl: null,
};
