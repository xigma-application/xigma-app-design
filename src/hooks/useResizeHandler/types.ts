export type TResizeHandlerSettings = {
  initialHeight: number;
  initialWidth: number;
  isInitiallyInvertedX: boolean;
  isInitiallyInvertedY: boolean;
  maxHeight: number;
  maxWidth: number;
  minHeight: number;
  minWidth: number;
};
